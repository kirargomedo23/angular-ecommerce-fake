import { ImageBlankDirective } from './image-blank.directive';

describe('ImageBlankDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageBlankDirective();
    expect(directive).toBeTruthy();
  });
});
